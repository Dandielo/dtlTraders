package net.dandielo.citizens.traders_v3.utils;

public class NBTUtils {	
	public static void addLore(org.bukkit.inventory.ItemStack bItem)
	{
	/*	CraftItemStack cItem = CraftItemStack.asCraftCopy(bItem);
	//	cItem.getItemMeta().setDisplayName("name");
	//	System.out.println(bItem.getItemMeta().getDisplayName());
		
		try 
		{
			// Get the Minecraft Vanila Item 
			Field handle = cItem.getClass().getDeclaredField("handle");
			// Set it accessible 
			handle.setAccessible(true);
			// Save the reference
			cItem.getItemMeta().setDisplayName("name");
			net.minecraft.server.v1_5_R3.ItemStack vItem = (net.minecraft.server.v1_5_R3.ItemStack) handle.get(cItem);

			System.out.print(vItem.getTag() != null);
			System.out.print(vItem.getTag() != null);
		} catch (Exception e) {
			e.printStackTrace();
			
		}*/
		/*
	}
	// Get the specific craftItem 
	CraftItemStack cItem = CraftItemStack.asCraftCopy(new ItemStack(0));

	try 
	{
		// Get the Minecraft Vanila Item 
		Field handle = cItem.getClass().getDeclaredField("handle");
		// Set it accessible 
		handle.setAccessible(true);
		// Save the reference
		net.minecraft.server.v1_5_R3.ItemStack vItem = (net.minecraft.server.v1_5_R3.ItemStack) handle.get(cItem);

		return vItem.getTag() != null;
	} catch (Exception e) {
		e.printStackTrace();*/
		
	}
}
